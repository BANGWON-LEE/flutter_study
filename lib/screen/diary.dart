import 'package:flutter/material.dart';

class DiaryBack extends StatelessWidget {
  const DiaryBack({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Column(
          children: [

          ],
        ),
      ),
    );
  }
}
